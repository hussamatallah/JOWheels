@model JOWheels.Models.Condition

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<!-- CSS File -->
<link href="~/styles/inputstyle/inputstyle.css" rel="stylesheet" />


<!-- Heading -->
<h1 class="text-center">Add Condition</h1>


<hr />


<!-- Center the form -->
<div class="row justify-content-center">
    <div class="col-md-6">
        <form asp-action="Create" id="conditionForm">

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group text-center mb-3" >
                <label asp-for="State" class="control-label"></label>
                <input asp-for="State" class="form-control" id="inp" required />
                <span id="StateError" class="text-danger"></span>
            </div>

            <div class="form-group d-flex justify-content-center mb-3">
                <input type="submit" value="Create" class="btn btn-primary mx-2" style="background-color:#007A3D" />
                <a asp-action="Index" class="btn btn-secondary mx-2">Back to List</a>
            </div>

        </form>
    </div>
</div>


<!-- A links For Including jQuery -->
<script src="~/lib/jquery/dist/jquery.min.js"></script>


@section Scripts {
    <script>
        $(document).ready(function () {
            $("#conditionForm").submit(function (event) {
                var isValid = true;
                var stateInput = $("#inp").val();

                if (stateInput === "") {
                    $("#StateError").text("Can't be NULL");
                    isValid = false;
                } else {
                    // If the input is not empty, clear any previous error message
                    $("#StateError").text("");
                }

                // If the form is invalid, prevent form submission
                if (!isValid) {
                    event.preventDefault();
                }
            });
        });
    </script>
}